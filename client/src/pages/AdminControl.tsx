import { useMemo, useState } from "react";
import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Switch } from "@/components/ui/switch";
import { trpc } from "@/lib/trpc";
import { filterAndSortAdminMembers, type AdminMemberRoleFilter, type AdminMemberSortOption } from "@shared/membership";
import { ArrowDownAZ, CalendarClock, Eye, Search, ShieldCheck, SlidersHorizontal, UsersRound } from "lucide-react";

const featureLabels = {
  ordersEnabled: ["Yeni siparişler", "Müşterilerin yeni sipariş oluşturmasına izin ver"],
  courierPortalEnabled: ["Kurye üyeliği", "Yeni kurye hesabı seçimini etkinleştir"],
  storePortalEnabled: ["Mağaza üyeliği", "Yeni mağaza hesabı seçimini etkinleştir"],
  liveTrackingEnabled: ["Canlı takip", "Teslimatların anlık konum takibini etkinleştir"],
} as const;

const roleLabels = { all: "Tüm roller", user: "Müşteri", courier: "Kurye", store: "Mağaza", accountant: "Muhasebe", admin: "Yönetici" } as const;
const sortLabels = { recently_active: "Son girişe göre", newest: "Üyelik tarihine göre", name_asc: "İsim A–Z", name_desc: "İsim Z–A" } as const;

function formatDate(date: Date | null | undefined) {
  return date ? new Intl.DateTimeFormat("tr-TR", { dateStyle: "medium", timeStyle: "short" }).format(date) : "—";
}

export default function AdminControl() {
  const { user, isAuthenticated } = useAuth();
  const [memberQuery, setMemberQuery] = useState("");
  const [roleFilter, setRoleFilter] = useState<AdminMemberRoleFilter>("all");
  const [sortOption, setSortOption] = useState<AdminMemberSortOption>("recently_active");
  const overview = trpc.admin.overview.useQuery(undefined, { enabled: isAuthenticated && user?.role === "admin" });
  const utils = trpc.useUtils();
  const setFeature = trpc.admin.setFeature.useMutation({ onSuccess: () => utils.admin.overview.invalidate() });
  const setMemberRole = trpc.admin.setMemberRole.useMutation({ onSuccess: () => utils.admin.overview.invalidate() });
  const members = useMemo(() => filterAndSortAdminMembers(overview.data?.members ?? [], { query: memberQuery, role: roleFilter, sort: sortOption }), [memberQuery, overview.data?.members, roleFilter, sortOption]);
  const clearMemberFilters = () => { setMemberQuery(""); setRoleFilter("all"); setSortOption("recently_active"); };

  if (!isAuthenticated || user?.role !== "admin") {
    return <DashboardLayout><main className="mx-auto max-w-3xl px-4 py-12"><Card className="rounded-3xl border-0 shadow-sm"><CardContent className="py-10 text-center"><ShieldCheck className="mx-auto mb-4 h-10 w-10 text-slate-400"/><h1 className="text-xl font-black">Yönetici erişimi gerekli</h1><p className="mt-2 text-sm text-slate-600">Bu panel yalnızca Run Courier yöneticileri için kullanılabilir.</p></CardContent></Card></main></DashboardLayout>;
  }

  return <DashboardLayout><main className="mx-auto max-w-6xl px-4 py-8"><div className="mb-7"><Badge className="mb-3 bg-orange-100 text-orange-800 hover:bg-orange-100">Yönetici merkezi</Badge><h1 className="text-3xl font-black tracking-tight text-slate-950">Kolay yönetim paneli</h1><p className="mt-2 text-sm text-slate-600">Hizmet modüllerini tek tıkla yönetin; müşteri, kurye ve mağaza üyeliklerini filtreleyip sıralayın.</p></div><div className="grid gap-5 lg:grid-cols-[.85fr_1.4fr]"><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2"><SlidersHorizontal className="h-5 w-5 text-[#e54725]"/>Tek tıkla modül yönetimi</CardTitle></CardHeader><CardContent className="space-y-3">{overview.data ? Object.entries(featureLabels).map(([key, [title, description]]) => <div key={key} className="flex items-center justify-between gap-3 rounded-2xl bg-slate-50 p-4"><div><p className="font-bold text-slate-900">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{description}</p></div><Switch checked={overview.data.featureSettings[key as keyof typeof overview.data.featureSettings]} disabled={setFeature.isPending} onCheckedChange={enabled => setFeature.mutate({ key: key as keyof typeof featureLabels, enabled })} aria-label={`${title} aç/kapat`}/></div>) : <p className="text-sm text-slate-500">Ayarlar yükleniyor…</p>}</CardContent></Card><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2"><UsersRound className="h-5 w-5 text-[#e54725]"/>Üyelikler ve roller</CardTitle><p className="text-sm font-normal text-slate-500">Kullanıcı rollerini güncelleyin; filtreler yalnızca bu yönetim oturumunda görünümü değiştirir.</p></CardHeader><CardContent>{overview.data ? <><div className="grid gap-2 rounded-2xl bg-slate-50 p-3 sm:grid-cols-2 lg:grid-cols-[minmax(0,1fr)_150px_170px]"><div className="relative sm:col-span-2 lg:col-span-1"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400"/><Input value={memberQuery} onChange={event => setMemberQuery(event.target.value)} placeholder="Ad, e-posta veya telefon ara" className="h-10 bg-white pl-9" aria-label="Üyeliklerde ara"/></div><select value={roleFilter} onChange={event => setRoleFilter(event.target.value as AdminMemberRoleFilter)} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm" aria-label="Üyelik rolüne göre filtrele">{Object.entries(roleLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><select value={sortOption} onChange={event => setSortOption(event.target.value as AdminMemberSortOption)} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm" aria-label="Üyelikleri sırala">{Object.entries(sortLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></div><div className="mt-3 flex items-center justify-between gap-3"><p className="text-xs font-semibold text-slate-500"><span className="text-slate-900">{members.length}</span> / {overview.data.members.length} üye gösteriliyor</p><Button type="button" variant="ghost" size="sm" onClick={clearMemberFilters} disabled={!memberQuery && roleFilter === "all" && sortOption === "recently_active"} className="text-xs text-[#c9381b]">Filtreleri temizle</Button></div><div className="mt-4 max-h-[540px] overflow-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="sticky top-0 bg-white text-xs text-slate-500"><tr><th className="pb-3">Kullanıcı</th><th className="pb-3">İletişim</th><th className="pb-3"><span className="inline-flex items-center gap-1"><CalendarClock className="h-3.5 w-3.5"/>Son giriş</span></th><th className="pb-3">Rol</th><th className="pb-3">İşlem</th></tr></thead><tbody>{members.map(member => <tr key={member.id} className="border-t border-slate-100"><td className="py-3"><p className="font-semibold text-slate-900">{member.name || "İsimsiz"}</p><p className="mt-0.5 text-[11px] text-slate-400">Üyelik: {formatDate(member.createdAt)}</p></td><td className="py-3 text-slate-500">{member.email || member.phone || "-"}</td><td className="py-3 text-xs text-slate-500">{formatDate(member.lastSignedIn)}</td><td className="py-3"><Badge variant="outline">{roleLabels[member.role]}</Badge></td><td className="py-3"><select className="h-9 rounded-lg border border-slate-200 bg-white px-2 text-xs" value={member.role} disabled={member.role === "admin" || setMemberRole.isPending} onChange={event => setMemberRole.mutate({ userId: member.id, role: event.target.value as "user" | "courier" | "store" | "accountant" })} aria-label={`${member.name || "Kullanıcı"} rolünü güncelle`}><option value="user">Müşteri</option><option value="courier">Kurye</option><option value="store">Mağaza</option><option value="accountant">Muhasebe</option>{member.role === "admin" && <option value="admin">Yönetici</option>}</select></td></tr>)}{members.length === 0 && <tr><td colSpan={5} className="py-10 text-center"><Search className="mx-auto mb-2 h-5 w-5 text-slate-300"/><p className="font-semibold text-slate-700">Eşleşen üyelik bulunamadı.</p><p className="mt-1 text-xs text-slate-500">Arama metnini veya rol filtresini değiştirin.</p></td></tr>}</tbody></table></div></> : <p className="text-sm text-slate-500">Üyelikler yükleniyor…</p>}</CardContent></Card></div><Card className="mt-5 rounded-3xl border-0 bg-slate-950 text-white shadow-sm"><CardContent className="flex items-start gap-3 py-5"><Eye className="mt-0.5 h-5 w-5 text-orange-300"/><p className="text-sm leading-6 text-slate-300">Arama, rol filtresi ve sıralama yalnızca yöneticinin ekranındaki üyelik görünümünü düzenler. Rol değişiklikleri ise yetkili sunucu prosedürü üzerinden kalıcı olarak uygulanır; yönetici rolü bu ekrandan değiştirilemez.</p></CardContent></Card></main></DashboardLayout>;
}
